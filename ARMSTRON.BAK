#include<stdio.h>
#include<conio.h>
void main()
{
int num,sum=0,r,org;
clrscr();
printf("\n enter the number");
scanf("%d",&num);
org=num;
while(num>0)
{
r=num%10;
sum=sum+r*r*r;
num=num/10;
}
if(sum==org)
printf("\n armstrong number");
else
printf("\n non armstrong number");
getch();
}